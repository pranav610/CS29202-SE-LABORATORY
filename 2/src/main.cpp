#include <bits/stdc++.h>
#include <nodes.hpp>
#include <ways.hpp>
#include <case1.hpp>

using namespace std;

int main()
{
    unordered_map<long long, nodes> nodesList;
    vector<ways> waysList;
    storeInput(nodesList, waysList);
}